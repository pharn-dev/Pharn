{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/resource-leak/evals/cases/case-open-no-close.md:16" }
    ],
    "semantic": [
      {
        "judge": "the fs.openSync handle bound on line 16 is detected deterministically by pharn/floor/scan-code-resource-leak.mjs as unclosed (no close call on the binding, no using, no finally) and reported as a FLOOR finding (rule_id P2, file = the binding line from the scanner); the lens may note in free-text whether a caller closes it elsewhere, but does NOT suppress the finding on that basis."
      }
    ]
  }
}
