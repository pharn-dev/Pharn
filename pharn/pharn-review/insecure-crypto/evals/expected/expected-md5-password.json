{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/insecure-crypto/evals/cases/case-md5-password.md:15" }
    ],
    "semantic": [
      {
        "judge": "the MD5 hashing primitive on line 15 is detected deterministically by .dev/floor/scan-code-crypto.mjs (kind weak-hash-md5) and reported as a FLOOR finding (rule_id P2, file = the primitive's line from the scanner); the lens may note in free-text that MD5 is unsuitable for password storage (Layer-2 advisory) but the enum-gated fields come from the scanner hit, not that judgment."
      }
    ]
  }
}
