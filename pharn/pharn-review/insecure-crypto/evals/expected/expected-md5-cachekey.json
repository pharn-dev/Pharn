{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/insecure-crypto/evals/cases/case-md5-cachekey.md:15" }
    ],
    "semantic": [
      {
        "judge": "the MD5 primitive on line 15 is detected deterministically (kind weak-hash-md5) and emitted as a FLOOR finding regardless of context; the lens's Layer-2 free-text MAY note the MD5 is a non-security cache/ETag key (plausibly benign) but MUST NOT suppress the finding on that basis — surface-don't-suppress at the Layer-1/Layer-2 boundary (a lens never gates)."
      }
    ]
  }
}
