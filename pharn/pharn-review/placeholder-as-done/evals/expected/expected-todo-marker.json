{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/placeholder-as-done/evals/cases/case-todo-marker.md:14" }
    ],
    "semantic": [
      {
        "judge": "the to-do comment marker on line 14 is detected deterministically by .dev/floor/scan-code-placeholder.mjs (Pass A marker membership) and reported as a FLOOR finding (rule_id P2, file = the marker line from the scanner); the lens may note in free-text whether the marker is a real gap or an intentional annotation, but does NOT suppress the finding on that basis."
      }
    ]
  }
}
