{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/missing-error-handling/evals/cases/case-unguarded-await.md:14" }
    ],
    "semantic": [
      {
        "judge": "scan-code-missing-error-handling.mjs reports found:true with one unguarded-await hit on line 14 (an awaited network call with no enclosing try/catch and no same-line .catch); the lens emits exactly one FLOOR finding whose file cites line 14. Whether this await truly needs handling here (vs a caller handling it) is advisory — surfaced for the human, never a basis to gate."
      }
    ]
  }
}
