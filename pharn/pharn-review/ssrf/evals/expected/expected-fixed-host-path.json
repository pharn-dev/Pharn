{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-review/ssrf/evals/cases/case-fixed-host-path.md:14" }
    ],
    "semantic": [
      {
        "judge": "the request-value-into-fetch on line 14 is detected deterministically by the scanner and reported as a FLOOR finding (rule_id P2, file = the sink line); the value appends only to the PATH of a fixed host, so the lens's Layer-2 free-text MAY note that full-host SSRF may not be reachable absent ../@/// tricks (plausibly lower-risk), but the finding is STILL emitted — the fixed-host nuance surfaces in free-text and does NOT suppress the scanner hit."
      }
    ]
  }
}
