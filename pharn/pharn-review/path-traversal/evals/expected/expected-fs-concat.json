{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/path-traversal/evals/cases/case-fs-concat.md:15" }
    ],
    "semantic": [
      {
        "judge": "the request-value-into-fs-path on line 15 is detected deterministically by .dev/floor/scan-code-path-traversal.mjs and reported as a FLOOR finding (rule_id P2, file = the sink line from the scanner); the lens may note in free-text whether the value is actually untrusted and whether sanitization (path.basename / allow-list / .. check) happens elsewhere, but does NOT suppress the finding on that basis."
      }
    ]
  }
}
