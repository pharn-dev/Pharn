{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn/pharn-review/injection/evals/cases/case-sql-concat.md:15" }
    ],
    "semantic": [
      {
        "judge": "the concat-into-query on line 15 is detected deterministically by pharn/floor/scan-code-injection.mjs and reported as a FLOOR finding (rule_id P2, file = the sink line from the scanner); the lens may note in free-text whether the operand is actually untrusted and whether sanitization happens elsewhere, but does NOT suppress the finding on that basis."
      }
    ]
  }
}
