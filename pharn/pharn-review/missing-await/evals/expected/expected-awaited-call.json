{
  "skill_kind": "llm",
  "assertions": {
    "structural": [{ "kind": "finding_count", "op": "==", "value": 0 }],
    "semantic": [
      {
        "judge": "the async call is `await`ed (`await loadUser(req.id);`), so the statement line begins with `await`, not the callee — it is not the statement-head bare-call shape, so .dev/floor/scan-code-missing-await.mjs reports found:false; the lens emits NO finding and does not manufacture one. A clean scan is not proof the code is async-correct (Layer-1 bound), only that no floating-statement-call shape was detected."
      }
    ]
  }
}
