{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-review/secrets-in-code/evals/cases/case-hardcoded-key.md:14" }
    ],
    "semantic": [
      {
        "judge": "the AWS-access-key-shaped literal on line 14 is detected deterministically by .dev/floor/scan-code-secrets.mjs and reported as a FLOOR finding (rule_id P2, file = the literal's line from the scanner); the lens may note in free-text that it is AWS's documented EXAMPLE key (a placeholder) but does NOT suppress the finding on that basis."
      }
    ]
  }
}
