{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-review/missing-error-handling/evals/cases/case-unguarded-json-parse.md:14" }
    ],
    "semantic": [
      {
        "judge": "scan-code-missing-error-handling.mjs reports found:true with one unguarded-json-parse hit on line 14 (a parse of external text that throws on malformed input, with no enclosing try/catch); the lens emits exactly one FLOOR finding whose file cites line 14. Whether raw is always well-formed here is advisory — surfaced, never a basis to gate."
      }
    ]
  }
}
