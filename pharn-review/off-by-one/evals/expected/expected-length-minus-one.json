{
  "skill_kind": "llm",
  "assertions": {
    "structural": [{ "kind": "finding_count", "op": "==", "value": 0 }],
    "semantic": [
      {
        "judge": "although a `<=` appears, its right operand is `buf.length - 1` (arithmetically corrected), not a bare `.length`, so .dev/floor/scan-code-off-by-one.mjs reports found:false; the lens emits NO finding and does not manufacture one. A clean scan is not proof the code is boundary-safe (Layer-1 bound), only that no bare-`.length` `<=` shape was detected."
      }
    ]
  }
}
