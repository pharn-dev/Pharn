{
  "skill_kind": "llm",
  "assertions": {
    "structural": [
      { "kind": "finding_count", "op": "==", "value": 1 },
      { "kind": "field_equals", "field": "type", "value": "FINDING" },
      { "kind": "field_equals", "field": "rule_id", "value": "P2" },
      { "kind": "field_equals", "field": "severity", "value": "important" },
      { "kind": "file_resolves", "value": "pharn-review/n-plus-one/evals/cases/case-braceless-map-query.md:13" },
      { "kind": "needle_absent_from_enum_gated", "needle": "db.findMany" }
    ],
    "semantic": [
      {
        "judge": "the finding is justified by the `db.findMany(...)` call inside the braceless `.map` callback on line 13 (detected via the `.map` call-argument paren interval), a query issued once per user"
      }
    ]
  }
}
