node_modules
package-lock.json
.dev/floor/test-fixtures
pharn/floor/test-fixtures

# Generated capability catalog: byte-for-byte output of `npm run docs:generate`, guarded by
# .dev/floor/check-capability-catalog.mjs. Excluded so the formatter can never rewrite committed
# bytes and make the drift checker see false drift (committed must equal the generator's raw output).
docs/capabilities/

# Generated lessons index: byte-for-byte output of `npm run docs:generate`, guarded by
# .dev/floor/check-lessons-index.mjs. Same reason as the catalog above — and a sharper one here: its rows
# reproduce canon lesson TITLES verbatim, several of which carry back-ticks and `||` (lessons-learned.md
# L15), so any reformatting pass would rewrite committed bytes and the drift checker would see false drift.
docs/lessons-index.md

# Tool-managed Claude/Cursor settings (not project source)
.claude/settings.json
.claude/settings.local.json

# Trusted spec docs: human-only, hook-protected. Excluded from the formatter
# so style fixes can never mutate their content-hash (fix #4 / spec-drift).
# They may still be formatted by hand; they are out of the formatter's scope only.
# CONSTITUTION.md + ARCHITECTURE.md relocated under pharn/ (runtime-layout move); THREAT-MODEL.md +
# LIMITS.md stay at root (dev docs, not installed).
pharn/CONSTITUTION.md
pharn/ARCHITECTURE.md
THREAT-MODEL.md
LIMITS.md
